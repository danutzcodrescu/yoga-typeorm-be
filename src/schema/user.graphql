type User {
  id: String
  username: String
  email: String
  password: String
  createdAt: String
  updatedAt: String
}

type Query {
  users: User
}

type Mutation {
  register(email: String, username: String!, password: String): User
}
